window.addEventListener("DOMContentLoaded", async () => {
            const MODEL_URL = './model.json';
            const model = await tf.loadLayersModel(MODEL_URL);

            let consumption_text = document.getElementById("generatedText"");
            consumption_text.innerHTML = "Тепер можете згенерувати текст";

            let button = document.getElementById("predict");

            button.addEventListener("click", () => {
                const input_text = document.getElementById("inputText").value;
           
            });
        });
